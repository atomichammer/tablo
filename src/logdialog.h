#ifndef LOGDIALOG_H
#define LOGDIALOG_H

#include <QDialog>

class LogDialog : public QDialog
{
    Q_OBJECT
public:
    explicit LogDialog(QWidget *parent = 0);

signals:

public slots:

};

#endif // LOGDIALOG_H
